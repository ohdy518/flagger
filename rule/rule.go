package rule

func keyRule() {
	/*
		Rules for key names("key").

		key should not contain whitespace.
		key should only contain latin alphabet and numbers.
		key should not start with numbers.
	*/
}
